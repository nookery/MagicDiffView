---
description: 代码修改后必须运行单元测试，新特性需要补充单元测试
globs: *.swift
alwaysApply: true
---
# 测试要求规则

本规则确保代码质量和稳定性，要求在代码修改后必须运行单元测试，并在添加新特性时补充相应的单元测试。

## 基本要求

### 代码修改后必须运行测试

每次修改代码后，都必须运行完整的单元测试套件来确保：

1. **现有功能未被破坏**：确认修改没有引入回归问题
2. **新修改正常工作**：验证修改的代码按预期运行
3. **代码质量保持**：确保测试覆盖率不下降

### 测试运行方式

- 使用 `swift test` 命令运行所有单元测试
- 确保所有测试都通过（绿色状态）
- 如果有测试失败，必须先修复问题再提交代码

## 新特性补充测试

### 何时需要补充测试

添加新功能时必须补充相应的单元测试：

1. **新方法/函数**：为新增的公共方法编写单元测试
2. **新类/结构体**：为新增的类型编写测试用例
3. **新初始化方法**：测试不同的初始化场景
4. **错误处理**：测试异常情况和错误处理逻辑
5. **边界条件**：测试边界值和边缘情况

### 测试覆盖要求

新特性测试应覆盖：

- **正常情况**：期望的输入和输出
- **异常情况**：无效输入、错误条件
- **边界条件**：最小值、最大值、空值等
- **性能考虑**：如适用，测试性能相关场景

## 测试编写指南

### 单元测试命名规范

- 测试方法以 `test` 开头
- 使用描述性名称，说明测试的具体行为
- 示例：`testParseUnifiedDiffWithValidInput`、`testInitializationWithEmptyDiffOutput`

### 测试结构

```swift
func testFeatureName() {
    // Given: 准备测试数据和环境
    let input = "test input"
    let expected = "expected output"

    // When: 执行被测试的代码
    let result = someFunction(input)

    // Then: 验证结果
    XCTAssertEqual(result, expected)
}
```

### 测试数据准备

- 使用真实的测试数据或模拟数据
- 为复杂场景准备专门的测试数据文件
- 考虑使用 `DemoFiles` 目录中的示例数据

## 工作流程

### 开发流程

1. **编写/修改代码**
2. **运行现有测试**：确保修改没有破坏现有功能
3. **补充新测试**：为新特性编写测试用例
4. **运行完整测试套件**：验证所有测试都通过
5. **代码审查**：确保测试覆盖充分

### 持续集成

- 在 CI/CD 流程中自动运行测试
- 只有所有测试通过才能合并代码
- 监控测试覆盖率变化

## 示例场景

### 添加新初始化方法

当为 `MagicDiffView` 添加 `diffOutput` 初始化方法时：

1. **编写初始化测试**：
   ```swift
   func testInitWithDiffOutput() {
       let diffOutput = """
       diff --git a/test.txt b/test.txt
       @@ -1 +1 @@
       -old
       +new
       """
       let view = MagicDiffView(diffOutput: diffOutput)
       // 验证初始化成功
   }
   ```

2. **测试边界情况**：
   ```swift
   func testInitWithEmptyDiffOutput() {
       let view = MagicDiffView(diffOutput: "")
       // 验证空输入的处理
   }
   ```

3. **运行所有测试**确保没有回归

## 注意事项

- **测试优先级**：优先编写核心功能测试，再补充边缘情况
- **测试维护**：定期审查和更新测试用例
- **性能测试**：对于性能敏感的代码，考虑添加性能测试
- **集成测试**：复杂交互可能需要集成测试补充

遵循此规则将确保代码库的稳定性和可维护性，提高开发效率并减少生产环境的问题。